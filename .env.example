# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Replace with your actual Supabase PostgreSQL connection string
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Direct connection to the database using Supabase project reference
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# Direct connection to the database. Used for migrations.
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"
        
# Supabase configuration
# Get these values from your Supabase project settings > API
VITE_SUPABASE_URL="https://[YOUR-PROJECT-REF].supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# Supabase service role key for admin operations (e.g., seeding users)
# WARNING: This key has admin privileges and should be kept secret
# Get this from your Supabase project settings > API
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Base URL for redirects (used for email confirmation)
VITE_BASE_URL="http://localhost:3000"

# Note: You can get these URLs from Supabase:
# 1. Go to your Supabase project dashboard
# 2. Go to Project Settings > Database
# 3. Find "Connection string" and "Connection pooling"
# 4. Use the CONNECTION_POOLING string for DATABASE_URL
# 5. Use the DIRECT_CONNECTION string for DIRECT_URL
